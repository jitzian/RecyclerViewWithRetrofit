package net.sgoliver.android.navigationdrawer.observaleDummy;

import android.util.Log;

import java.util.Observable;


/**
 * Created by User on 10/18/2016.
 */

public class MyModelObservable extends Observable {
    public static final String TAG = MyModelObservable.class.getName();
    /**
     * Creates an Observable with a Function to execute when it is subscribed to.
     * <p>
     * <em>Note:</em> Use {@link #create(OnSubscribe)} to create an Observable, instead of this constructor,
     * unless you specifically have a need for inheritance.
     *
     * @param f {@link OnSubscribe} to be executed when {@link #subscribe(Subscriber)} is called
     */





}
